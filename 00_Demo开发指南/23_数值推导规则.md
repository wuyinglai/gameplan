# 23 · 数值推导规则（算法层 · 只写「怎么算」，不写「算出来是多少」）

> 《余烬商队》·《Demo 程序编写指导》· **非编号正文，是"算法层"**
> **状态：🔶 v2 重构（2026-09-22）。本文件不定义游戏，也不记任何当前数值，只写"数是怎么算出来的、为什么这么算"。**
> **用途：改掉任何一个锚点后，按本文件的公式与 §11 的顺序重算一遍，全库数值即同步 —— 不必重新推演。**
>
> 🔴 **本文件为什么一个具体数值都不留**（v2 的核心改动）：
> 数值分三类，混在一起就会出事 ——
>
> | 类别 | 是什么 | 住在哪 | 谁能改 |
> |:--|:--|:--|:--|
> | **锚点**（输入） | 定死的基准：体力、速度、薪资、粮价… | `04 §0.1 锚表` | **只改锚表** |
> | **规则**（算法） | 由锚点算出其余数值的**等式与理由** | **本文件** | 改机制时才改 |
> | **求值结果**（输出） | 某次算出来的具体数字：人数、硬账、单价、净利… | 各系统文件（`04` 各节 / `0A` / `14` / `08`） | **由 §11 重算得出，不手改** |
>
> 若把"求值结果"抄进本文件，下次改锚点时就会看到一堆不知来历的数字，**分不清哪个是规则、哪个是上一版的结果** → 该改的没改、不该动的动了。所以本文件只留**等式与理由**，所有具体数字一律**指向锚表**。
> 文中出现的字母（`S` `c` `H` `U`…）都是**符号**；出现的少数常数（倍率、系数）是**机制的一部分**（改它等于改机制），已在文中标明。
>
> 口径日期：2026-09-22。

---

## §0 怎么用这份文件

1. **要改数 → 只去 `04 §0.1 锚表`**，别处不许另拍。
2. **要改机制 → 改本文件的等式**，然后在 §12 登记。
3. **改完 → 按 §11 从第 1 步跑到底**，把结果回写到各系统文件（公式不动，只换结果）。
4. **两边打架时 → 先验算**：`04` / `0A` 里的数如果和本文件算法不符，是"结果过期"，改结果那一方。

---

## §1 符号约定（全文件通用 · 取值一律查锚表）

| 符号 | 含义 | 单位 | 备注 |
|:--|:--|:--|:--|
| `S` | 每天野外体力（由**动力**决定） | 点 | 取全队战斗人员**最低值** |
| `c` | 走 1 格消耗（由**车轮**决定） | 点/格 | |
| `c_侦` | 侦察一次的耗点 | 点 | 固定，不随车轮变 |
| `c_事` | 采集 / 休整一次的耗点 | 点 | 与侦察同价 |
| `v` | 每天走几格（标准打法） | 格/天 | `v = f(S, c, c_侦)` |
| `q` | **单车**载格数（升级轴） | 格/辆 | |
| `q0` | 基准单车格数（`q ÷ q0` = 车格数倍数） | 格/辆 | 由锚表定 |
| `K` | 车数 = **车夫数** | 辆 = 人 | 两者恒等 |
| `K_耐` | 计耐久的货车数 | 辆 | 单车不计 |
| `P` | 总人数 | 人 | |
| `P_车` `P_战` `P_特` `P_后` | 各岗人数 | 人 | 车夫 / 护卫 / 特殊 / 后勤 |
| `H` | 日均硬账 | 金/天 | |
| `U` | 档位固定单价 | 金/格 | |
| `m` | 单价倍率 | — | **机制常数**，现行取 1.8 |
| `T₁…T₄` `G₁…G₄` | 四档的**天数 / 格数** | 天 / 格 | 档位定义查锚表 |
| `d_n` | 第 n 岗日薪 | 金/人·天 | 四岗见 §3.3 |
| `W0` | 基准车满仓续航常数（`q = q0`、且途中无中继点时可跑的里程） | 格 | §2.6 |
| `f` | 每人每天消耗的份数（食 / 水各 1 份，锚 A7） | 份/人·天 | §4 |
| `p_粮` `p_水` | 粮价 / 水价 | 金/份 | §4.1 |
| `r_补` | 沿途可免费补给的水占比 | — | §4.1 |
| `ρ_食` `ρ_水` | 食 / 水的舱密度（1 格装几份） | 份/格 | `ρ_食` 可升级 · `ρ_水` 固定 |
| `u_损` | 单辆计耐久车的日均车损 | 金/辆·天 | §5.2 |

> **符号的作用**：改锚点时不碰本文件；本文件里永远不会出现"上一版的 14 人"这类残留。

---

## §2 世界与移动

### 2.1 每天走几格（移动主式）

```
v = round( (S − c_侦) / c )
```

**三个设计理由**（为什么是这个形状）：

| 形状 | 理由 |
|:--|:--|
| **先减侦察再除** | 侦察是"看清路"的前置动作，**先扣掉才不会出现"侦察完就走不动"** |
| **除法，不是加法** | 能量与消耗是「**总量 ÷ 单价**」的商品关系 —— 这正是不停车/换车轮能"多挤出格"的数学基础 |
| **取整（round）** | 格是整数，走不了半格 → **余数天然浪费掉**，于是"每格再省一点"就能跨过整数关口多走一格（升级手感的来源） |

### 2.2 两个旋钮：加能量 / 降消耗（**不能压成一个轴**）

| 轴 | 谁提供 | 改哪个符号 | 玩家感觉 |
|:--|:--|:--|:--|
| **加能量** | 动力（驼兽 / 旧文明引擎） | 抬高 `S` | 一天的总劲变大 |
| **降消耗** | 车轮（改良 / 精制） | 压低 `c` | 每格花得更省 |

**为什么要分成两个轴**：
- 若全做成"降消耗"，后期就只剩一条升级线，**引擎和车轮会退化成同一件东西**；
- 分开后，两件装备各有自己的「**做不到的事**」——动力不省每格、车轮不给总量 → 玩家可以**只买一个**，这是真实选择（呼应铁律 14）；
- 数学上两者**近似相乘**（`v = (S − c_侦) / c`）：任一轴变好，收益都放大约一倍；两轴同时变好 ≈ 4 倍。**所以后期"引擎 + 精制轮"才最猛**，但缺一个都退半。
  > ⚠️ 严格说不是乘积：侦察预算是**先扣**的固定项（§2.1），所以 `S` 翻倍**不会**让 `v` 精确翻倍 —— 这是"先减后除"的必然后果，算收益时要按原式代值，别直接乘。

> 📌 **口径来源**：2026-09-22 用户裁定「不要全部都是降消耗，或者全部都是加能量」。
> ⚠️ 配套代价：**引擎占车厢格 + 耗油**（油走"按格"消耗，见 §5.4）；**驼兽沿途自采、不耗粮**（不进硬账）。

### 2.3 野外动作的耗点关系（规则）

一天的能量 `S` 是一个**预算**，玩家自己分配：

```
走 1 格 = c   ｜   侦察 1 次 = c_侦   ｜   采集 / 休整 1 次 = c_事
```

- `c_侦`、`c_事` **固定**，不随车轮变 —— **情报与"歇一口气"的价值不该跟着车轮打折**；
- `c_事` 取与侦察同价：**"做一件事" ≈ "换一格路"**，这条等值让玩家在"赶路 / 干活"之间有一个干净的取舍；
- **"标准打法"的定义**：预先留出一次侦察的预算，剩下的走格 —— `v` 就是按这个口径算的。

### 2.4 车辆：从哪来、怎么分工

```
车队 = 货车（载货 · 计耐久） + 单车（载人 · 不计耐久 · 不载货）
```

| 车种 | 干什么 | 为什么这样分 |
|:--|:--|:--|
| **货车** | 货舱的**唯一**来源；有耐久；归零抛锚（需修回阈值才能走） | 把"货"变成可被打击的目标 → 战斗失败会真的伤到生意 |
| **单车** | 载人；**不载货、不计耐久、不被攻击** | 把"人的运载"从战损里摘出来 —— **挨打掉的是货，不是人** |

> 载重与货舱的换算、单车/货车的具体格数：**查 `0A 第6A类 §14` 与锚表**。

### 2.5 扩容与升级（两条路，你点名要的规则）

```
横向扩容 = 加挂货车      →  K 增加，每辆 +q 格 / +耐久上限
纵向升级 = 单车格数提升   →  q 逐级上升（每级格数与**级数上限**由锚表定）
```

| 路 | 改什么 | 被谁闸住 | 为什么必须有这条闸 |
|:--|:--|:--|:--|
| **扩容（加车）** | 车数 `K` | **车数上限**（`04 §0.7.4`） | 车数 = 车夫数 = 人数 → **多加一辆车，等于多养两个人**（见 §3.2），成本自动反弹 |
| **升级（加格）** | 单车格数 `q` | **级数天花板** | 少了它，"车数上限"就不再是闸 —— **车数上限 × 无限格 = 无限货舱** |

**为什么要两条路并存**（这是 `04 §0.7.4` 的结论，不是新设计）：
> 消耗本身**挡不住无限扩容**，必须靠"车"来闸。但闸只卡横向（车数），纵向（格数）会从旁边漏 —— 所以纵向必须配一个**自己的天花板**。

**连带（很重要）**：`q` 不只影响装货，还决定**能走多远** ——

```
可及路程 ∝ q          （载得多 → 补给带得多 → 走得远；见 §2.6）
```

> 也就是说：**"升级车辆"是玩家唯一的"远行能力"旋钮**，车数被上限按着、编制被后勤规则按着 —— 想跑更远，只能升 `q`。

### 2.6 距离与可达

```
总续航   = W0 × (q ÷ q0) × K / (K + P_战 + P_特)     W0 = 基准车满仓续航常数 · q0 = 基准单车格数（均查锚表）
单程可达 = 总续航 ÷ 2                          ← 远征要往返，去 + 回
补给占货舱 ≈ 天数 × 系数 × (1 + (P_战 + P_特) / K)
```

**为什么是这个形状**：

| 项 | 理由 |
|:--|:--|
| `K / (K + P_战 + P_特)` | 战斗与特殊**占位但不拉货** → 每带 1 个，就割掉一份货舱 → **直接换算成距离损失**。这就是"带护卫是要付代价的" |
| `÷ 2` | 远征是**往返**：去程带上全部补给，回程要么带货、要么空跑 → 单程上限约等于总续航的一半 |
| 按**格数**算、不按**天数**算 | 否则装了动力就能摸到世界另一头，**"太远养不起"这条核心张力会崩** |

### 2.7 订单期限

```
期限 = (格数 ÷ 基准速度) × 1.3
```

`1.3` = **30% 缓冲**：允许绕路、休整、接顺路单而不至于超期。**迟到 ≠ 失败**（铁律 7）。

---

## §3 编制与人力

### 3.1 三条铁律（推导的起点）

```
① 车夫   = K                              （硬 · 跟着车走）
② 后勤   = 车夫 + 战斗 + 特殊               （硬）
③ 总人数 = 2 × (车夫 + 战斗 + 特殊)
```

### 3.2 推导方向：**车 → 车夫 → 总人数**（单向，不可反推）

| 步 | 关系 | 为什么是这条 |
|:--|:--|:--|
| 1 | **车夫 = 车数** | 一辆车必须一个人开（物理约束）。**"多少车夫"和"多少辆车"是同一个数** —— 用户问的"车夫多少人之后会有多少辆车"，答案就是恒等 |
| 2 | **后勤 = 占编人数之和** | 每个"开车 / 战斗 / 干专业活"的人，背后都要配一个后勤（做饭、修车、装卸、照料） → **1 : 1** |
| 3 | **总人数 = 2 × 占编人数** | 上一条的直接推论 |

🔴 **方向和数量同等重要**：**不能从"我想带多少人"出发**。想多带一辆车 ⇒ 必须先加 **1 个车夫 + 1 个后勤 = 2 人**，工资与补给随之翻上去。

> **为什么这样设计**：任何"堆规模"的冲动，都会**自动带上一倍的后勤成本** → 规模自我设限。这正是"距离即上限 / 管理成本"那套机制的**算式形态**（`04 §0.7.7`）。

### 3.3 工资怎么发

```
日薪总额 = Σ_岗 ( 该岗当前人数 − 初期核心该岗人数 ) × 该岗日薪 d_n
```

| 要点 | 规则 | 理由 |
|:--|:--|:--|
| **四档排序** | 护卫 > 特殊 > 车夫 > 帮工 | **按风险与技能定价**：护卫拿命换钱、特殊靠手艺、车夫是常规体力、帮工最低 |
| **初期核心不领薪** | 开局那批人**不计入工资** | 他们是**合伙**不是雇员 → 开局不被工资压死，同时让"招人"这一步产生**真实代价** |
| **用增量、不用全额** | 只算"超出初期核心"的那部分 | **老账不重算** → 以后改锚点，不会连开局的人一起被算进去 |
| **欠薪怎么办** | **只闲置，绝不挖走** | 铁律 7：惩罚只能"**要价**"，不能**剥夺能力** |

> 日薪的具体数额 `d_n` 查锚表；各档岗位人数由 §3.2 推出。

### 3.4 编制为什么能自我约束

三条铁律串起来是一条**单链**：`车数 → 车夫 → 后勤 → 总人数 → 硬账`。改任何一头，全链跟着动（§11 的咬合表）。
**这就是"不用硬限制也能拦住规模"的实现方式** —— 玩家不是被系统禁止带 100 辆车，而是**带不起**（人要翻倍、要吃、要发薪）。

---

## §4 补给（**两笔账：钱 与 格**）

> **"份"有两面**：**钱 = 份数 × 单价**；**格 = 份数 ÷ 密度**。这两个面**各自有天花板**，必须分开算。

### 4.1 钱

```
食物费/天 = P × 份数 × 粮价
水费/天   = P × 份数 × (1 − 路上补水率) × 水价
补给钱/天 = 食物费 + 水费
```

- 份数的基准关系：**1 人 1 天 = 1 份食 + 1 份水**（锚 A7）；
- **水的 `(1 − 补水率)` 是关键**：水能在路上免费获取，粮不能 → 这是"**水便宜、但占地**"的来源。

### 4.2 格

```
食物格/天 = P × 份数 ÷ 密度_食        密度_食 可升级（包装/干货）
水格/天   = P × 份数 × (1 − 补水率) ÷ 密度_水      密度_水 固定，永不升级
补给格/天 = 食物格 + 水格
```

**为什么食能升级、水不能**：食是干货，**可以压缩包装**（一份占越来越小的格）；水是液体，**装多少就是多少**。
→ 结果：**水成了吃格大户**（但只花很少的钱）。

> **密度升级的真正作用 = 抵消人数增长**：人数涨几倍，密度也涨几倍 → **每天占舱基本恒定**（≈货舱 25%）。所以"扩编到 3 倍"不会挤压带货空间 —— 这就是「密度可升级」这条设计线的存在理由。

### 4.3 为什么非要拆成两笔

| 笔 | 影响 | 天花板 |
|:--|:--|:--|
| **钱** | 利润（进硬账 → 进报价） | 玩家的资金 |
| **格** | **一趟能带多少货**（= 能赚多少） | 货舱总量 |

一笔涨、一笔可能不动（例：粮价涨 → 钱涨、格不变；密度升 → 格降、钱不变）。**混着算就再也分不清该动哪个旋钮。**

---

## §5 硬账（"一天到底必须花多少"）

### 5.1 四件套与边界

```
硬账 H = 补给 + 工资 + 车损 + 药包
```

| 进硬账（**必花**） | 不进硬账（**弹性**，有钱才花） |
|:--|:--|
| 补给（吃喝）· 工资 · 车损 · 药包 | 装备 · 招人 · 买车 · 情报 · 火种 |

**为什么划这条线**：报价的**分母只能是"不花就活不下去的钱"**。把装备钱也算进去 → 每次升级装备都会推高报价 → **价格失去锚**，越强越贵，循环打转。

### 5.2 车损怎么来

```
车损/天 = K_耐 × 单位车损        单位车损 = 地形损耗 + 战斗摊销
```

- **只算 `K_耐`（计耐久的货车）** —— 单车不计耐久、不被攻击，所以不进这笔；
- 战斗部分按"平均每场战斗掉多少耐久 ÷ 平均几场一次"摊到每天 → **战斗强度自动反映到日账上**。

### 5.3 药包怎么来

```
药包摊销 = 每场战斗的耗包 ÷ 平均几格遭遇一场 × 遭遇概率     ← 只跟"战斗人数"走，与总人数无关
```

**为什么跟战斗人数**：药包是**战斗消耗** —— 车夫和后勤不上场，就不该分摊这笔。
**为什么自变量是"格"而不是"天"**（与 §5.4 一致）：战斗是**走路走出来的**，走得快不会让遭遇变少 → 药包跟**里程**走。写"药包/天"只是把它折进日账的**表示法**，量的来源是里程。

### 5.4 按天 / 按格（**加速省的只是按天那份**）

| 归类 | 项目 | 为什么 |
|:--|:--|:--|
| **按天** | 吃喝 + 工资 | **人在就得吃** —— 与走多快无关 |
| **按格** | 车损 + **油耗** + 药包 | **走多远花多少** —— 加速省不掉 |

**这条划分是整个"动力系统"的平衡阀**：若不拆，速度一快、消耗占报酬比就会大幅下滑，**成本旋钮等于被关掉**，引擎与驼兽会强到没有取舍。
拆开之后：**加速只省得掉"按天"那份，"按格"那份照付。**
> 📌 **量纲约定**：`H` 恒为 **金/天**。所以「按格」那三项进 `H` 时，要**先用该档速度 `v` 折成日均**（`金/格 × v`）—— 折完数字会随速度变，那正是"加速省不掉"的体现。

---

## §6 单价与订单

### 6.1 档位固定单价

```
U_天 = 该档对应阶段的硬账 H × m         m = 单价倍率（机制常数，现行 1.8）
U_格 = U_天 ÷ 该档速度 v
```

**两条理由**：

| 设计 | 理由 |
|:--|:--|
| **固定单价（按档定，不按玩家阶段）** | 报价的分母是**硬账**；档位只决定"该档的硬账基准"。**玩家到了什么阶段不影响价目** → 消除"越练越贵"的荒谬感 |
| **换算成"金/格"** | 速度是**可变的**（动力/车轮）→ 只有按格报价，才能做到"**多跑多得、跑快不加钱**" |

### 6.2 为什么统一倍率 `m`（不分段递增）

早先版本用过递增倍率（越长的档越贵）。**作废**，理由：

> 递增倍率会让"**跑得长**"和"**跑得快**"两件事互相干扰 —— 陡度被单价提前吃掉，**通道就没了**。
> 统一 `m` 之后：**陡度 100% 由"叠加"提供**（见 §6.3），单价只干一件事 —— **覆盖该档成本 + 合理利润**，含义干净。

### 6.3 顺路叠加（**这才是玩家真实的钱**）

```
机制：同时持单无上限。一趟长线沿途经过各档边界 → 天然等于同时接了各档各一笔，路程只走一遍。

（下文 `Uₙ` 一律指第 n 档的 **`U_格`**；`Gₙ` 为该档格数；`天数` 用**该档阶段的速度 `v`** 折算，`H` 取同阶段的硬账）

短途档 报酬 = G₁ × U₁
中途档 报酬 = G₁ × U₁ + G₂ × U₂
长途档 报酬 = G₁ × U₁ + G₂ × U₂ + G₃ × U₃
超长档 报酬 = G₁ × U₁ + G₂ × U₂ + G₃ × U₃ + G₄ × U₄

天数    = 格数 ÷ v
消耗    = 天数 × H
净利    = 报酬 − 消耗
日净收益 = 净利 ÷ 天数
```

> ✅ **叠加是唯一的陡度来源**（判据，非数值）：**把叠加关掉，同单价的两档日净必须相等** —— 相等才对；若不等，说明公式写错了（§10 第 4 条）。

### 6.4 两个必然推论（条件式，非数值）

**① 规模越大，低档单自动接不动。**
```
当  天数 × H  >  报酬   ⇒   该档倒亏
```
规模一过某点，**短档在数学上自己消失**，玩家只剩长档可跑。这是"距离即上限"的**自动实现**，不需要额外规则。

**② 高档套利：人越少跑高档，赚得越多。**
档位单价是按"**该档该有的人数**"定的，但玩家实付的是**自己的人数**成本 → 若用更少的人跑到那个档，差额即套利。
**要不要堵**（堵法二选一：把该档的**天数下限**抬高 / 给档位配**最低车数**）—— 这是一条**机制决策**，决定后写进本文件 + `04 §8`。

---

## §7 支出端定价（**由日净收益反推，不拍脑袋**）

### 7.1 反推法

```
某物单价 = 该阶段日净收益 × 天数系数
```

**为什么不用"直接拍价格"**：日净收益本身**连续递增**（随人数与档位），所以价格自动跟着走；拍出来的价会在某个阶段**变成零钱**（同一把铁剑在 250 天路上还卖原价）。

### 7.2 天数系数表（**机制常数**，改它 = 改经济松紧）

| 项 | 系数 | 系数在说什么 |
|:--|:--|:--|
| 装备（单件） | **×12 天** | 约"两趟换一轮装"的节奏 |
| 加挂货车 | **×20 天** | 运力投资，回本约 20 天 |
| 火种 | **×15 天** | 只在军事基地出 → 天然后期价 |
| 招募（人） | **×25 天** | 含"日薪回本"意味 |

> 系数一变，全线价目跟着变。**这是经济松紧的唯一旋钮**。

### 7.3 装备走曲线、不走三档跳

**用户 2026-09-22 裁定**：因为"日净收益"本身连续，**价格自动连续** —— 中间档位插值即可，不必另发明规则。
**材质档（铁 → 钢 → 复合 → 特殊改造 → 旧时代）就是这条曲线的刻度。**

---

## §8 战斗（与移动**同刻度**，但**不通用**）

### 8.1 体力池

```
体力池 = Σ 各出战人体力        基准 / 人（有个体差异）
每回合回复 = Σ 各人体力
上限 = 2 × 回复
起手 = 满
```

| 招式档 | 微 · 轻 · 标准 · 重 · 极限（档位取值查锚表） |
|:--|:--|
| 消耗品（药包 / 零件） | 取"轻档"价位，**不占牌** |

> 🔴 **出牌没有上限**：起手 3 张（2 动作 + 1 记忆）**只是抽牌量**；回合内出牌 / 抽牌不受任何限制，**唯一闸门是体力池**。

### 8.2 伤害链

```
伤害 = 武器面板 × 招式倍数 × (1 + 角色加成%) × 攻击类型倍率
       →（护盾 → 屏障 → 护甲 → 生命）
```

| 攻击类型 | 无甲 | 软甲（布/皮） | 硬甲（铁/钢） |
|:--|--:|--:|--:|
| 打击 | ×0.95 | ×1.0 | **×1.3** |
| 斩击 | ×1.1 | **×1.2** | **×0.9** |
| 刺击 | ×1.1 | ×1.0 | ×1.0 |

> ⚠️ **倍率作用于整次伤害、一直进到生命**。**通用减伤率已取消**（会让相性被纯优乘数抹平）。
> **护甲**只由防具给（开战赋值 · 本场持久 · 战毕复位）；**护盾**只由盾牌给（回合末清空 · 可给队友叠）。

### 8.3 敌人数值

```
敌方总血池 = 我方血池 × 系数        普通 / 精英各一档（Boss 单独设计）
承伤 = 血池百分比 × 人数系数          普通 / 精英各一档
人数系数 = √(3 ÷ 出战人数)
```

> **个数只摊分总血池，不进总血公式**（3 只强盗 = 1 只灰烬兽，总血一样）。
> **回合数目标**：普通 / 精英 / Boss 各有一个区间（取值查锚表）；Boss **不锁血、单独设计、不可普通撤**。

### 8.4 野外自愈与心肺

| 项 | 算式 |
|:--|:--|
| **肺** | 战斗中**每回合体力回复** ×(1 + 每点加成 × 等级) |
| **心脏** | ① 体力**上限** ×(1 + 每点加成 × 等级)；② **战斗外每日自愈速率** |
| 每日自愈 | **随血池等比缩放**（血池越大，绝对回血越多，但天数比例不变） |

> 🔴 **野外体力不挂器官**（与 §2 解耦）—— 野外力气由"人（最低体力）+ 车（每格消耗）"两个旋钮决定，与肺/心脏无关。

---

## §9 体征盘 → 事件接口

```
全队该系统的战斗人员合计 ≥ 阈值  ⇒  事件多开一个选项
```

| 项 | 规则 |
|:--|:--|
| 人份 | 单格 5 点 · 运动对 10 点 |
| 判定口径 | **由事件自定**（例：倒树 = 全队合计；陷阱 = 踩上去那个人） |
| 只算谁 | **只算战斗人员**（车夫 / 帮工不计入） |
| 性质 | **硬阈值 · 不掷骰 · 永远有保底选项**（要价不剥夺） |

> 具体阈值在 `19` 配。**记忆 = 知识型解锁 · 体征 = 身体型解锁**，两者并列。

---

## §10 换锚点后的自检（**方法，不是样例数值**）

改完锚点、按 §11 算完，**必须过这五道闭合检查**。任何一道不闭合 → 说明改错了地方：

| # | 检查 | 判据 |
|:--:|:--|:--|
| 1 | **编制闭合** | 后勤 = 车夫 + 战斗 + 特殊；总人数 = 2 × 三者之和（**自己算出来的总数必须能反推回车数**） |
| 2 | **工资可复现** | **任取两档已落库的工资值**，代回 §3.3 公式都能算出原值 —— 差 1 金就是公式错了 |
| 3 | **硬账闭合** | `H` = 四件之和，且四件**每件都能独立算出**（不许有一项是"凑出来的差额"） |
| 4 | **叠加判据** | **关掉叠加，同单价两档的日净必须相等**；不等 = §6.3 写错了 |
| 5 | **单价互洽** | 同一档的 `U_格 × v` 必须 = `U_天`（两种写法同值） |

---

## §11 改锚点后的重算顺序（**本文件的核心用法**）

> **从第 1 步进，一路跑到第 12 步，不要跳。** 每步只依赖它上面的步骤。

```text
1. 锚表（04 §0.1）
   ├─ 改哪个锚，就只动哪个锚
   ↓
2. 移动链：S、c  →  v = round((S − c_侦) / c)
   ↓
3. 编制链：车数 K → 车夫(=K) → 后勤(=三者之和) → 总人数(=2×) → 计耐久车 K_耐
   ↓
4. 补给链：P → 补给钱/天（§4.1）· 补给格/天（§4.2）
   ↓
5. 工资链：各岗人数 − 初期核心 → × 日薪（§3.3）
   ↓
6. 车损链：K_耐 × 单位车损
   ↓
7. 药包：按 P_战 查表
   ↓
8. 硬账 H = 补给 + 工资 + 车损 + 药包
   ↓
9. 单价 U_格 = H × m ÷ v（逐档）
   ↓
10. 订单四档 = 叠加（§6.3）→ 净利 → 日净收益
   ↓
11. 支出端定价 = 日净收益 × 天数系数（§7.2）
   ↓
12. 可达距离：单程 = W0 × (q ÷ q0) × K ÷ (K + P_战 + P_特) ÷ 2
   ↓
   回写 04 / 0A / 14 / 08 的**求值结果**（公式不动）
```

**会互相咬合、必须一起改的几组**：

| 改了这个 | 必须同时重算 |
|:--|:--|
| 人数 `P` | 补给 · 工资 · 硬账 · 单价 · 四档 · 装备价（§4→§7 全链） |
| 车数 `K` | 车夫 · 总人数 · 计耐久车 · 车损 · 可达距离 |
| 单车格数 `q` | 可达距离 · 升级天花板（§2.5） |
| 速度 `v`（或 `S` / `c`） | 天数 · 消耗 · 日净 · 单价/格 · 油费 |
| 粮价 / 密度 / 补水率 | 补给钱（价）· 补给格（密度）· 硬账 · 单价 |
| 血池 / 承伤 | 敌人数值 · 回合数 · 药包摊销 |
| 硬账 `H` | 单价 · 四档净利 · **全套支出端定价** |

---

## §12 待办（本文件自身的尾）

| 项 | 状态 |
|:--|:--|
| 硬账拆「按天 / 按格」（§5.4） | ⚠️ 待拍 |
| 车辆扩容与车数上限的冲突（§2.5 / §3.2） | ⚠️ 待拍（上限是否放宽） |
| 支出端定价全套（§7） | ⚠️ 待落 `04 §9.7.8` / `0A 第6A类 §14` |
| 高档套利要不要堵（§6.4-②） | ⚠️ 待拍 |
| 事件检定阈值（§9） | ⚠️ 随 `19` 配 |
| 本文件在 `00_总纲` 索引里的条目 | ✅ 已补（编号 23，根 `README.md` 亦已挂指针） |
| 🔴 野外刻度的三处待同步 | ⚠️ 见 §12.1 |

### §12.1 野外刻度改版时的三个卡点（**不能只换数字**）

> 野外刻度（`S` / `c`）一改，**三处结构问题会一起暴露**，只换数字会让 `04` 内部更乱：

| # | 卡点 | 处理办法 |
|:--:|:--|:--|
| 1 | **采集 / 休整的耗点可能"放不下"** | 若"走 2 格 + 侦察"已用尽当天预算，则 `c_事` 必须降到与 `c_侦` 同级 —— **保持"一件事换一格路"的等值**（§2.3） |
| 2 | **车轮刻度要跟着翻新** | 车轮档位（标准 / 改良 / 精制）的耗点值是**按旧刻度定的**，换刻度后必须重排，使 `v` 仍能拉开三档 |
| 3 | **`0A 第1A类` 内部有矛盾** | §2 的"满速"与 §7.2 格距表的"满速"**互不一致** → 一并统一（此后"标准 / 满速"两个口径由 §2.1 唯一确定） |

**改野外刻度的全部影响面**（改前先照这张单子核对，别漏）：
`0A 第1A类`（§2 移动表 · §5 车轮表 · §7.2 格距）× `0A 第5A类 §1.1`（天气改每格消耗）× `04`（锚表 A1/A2 · §0.5 · §2 车轮表 · §2 注释 · §9 备注）× `08 DATA/TIME`（体力与移动常量）× `01 §5` × `12 §1` × `15 §1` × `18 §1` × `23 §2` × 根 `README` × `00_当前权威口径/README`。

---

*本文件是"算法层"：读它只该看到等式与理由，**不该看到任何"当前是多少"**。所有数值都可由 §11 从锚表重新求出来 —— 这也是它存在的唯一理由。*
